# Supabase project configuration for SellerFlow
project_id = "vvpwntehstjbccarqqzp"

[functions.moderatePost]
# verify_jwt is set to false so the Supabase API Gateway forwards the incoming
# user authentication token directly to the function for authoritative server-side verification.
verify_jwt = false
